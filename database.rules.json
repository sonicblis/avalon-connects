{
  "rules": {
    ".read": "auth != null",
    "accounts": {
      ".indexOn": ["attending/hostId", "settings/groupDisabled"],
      "$uid": {
        ".write": "auth.uid == $uid || auth.uid == 'fuTj3lqDkRfcHYuma2i4RgiHHu02'"
      }
    }
  }
}
